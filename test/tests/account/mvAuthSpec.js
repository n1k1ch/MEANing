/**
 * Created by n1k1ch on 15.03.2015.
 */
/*TODO: implement
describe('mvAuth', function(){

    var mockHttp;


    beforeEach(function() {
        module('app');

        mockHttp = sinon.stub({post: function(str, resp){
            console.log('calling stub post' );
            return {some: 'sssome'};
        }
        });

        module(function($provide) {
            $provide.value('$http', mockHttp);
        });
    });

    describe('authenticateUser', function() {

        it('should return false', inject(function(mvAuth) {
            expect(mvAuth.authenticateUser('alala', 'shalala'));//.should.eventually.equal(false);
        }));

    });

});
    */